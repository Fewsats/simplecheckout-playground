{
  "name": "simplecheckout-playground",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "npm run build && npx serve dist -l tcp://0.0.0.0:${PORT:-3000}"
  },
  "dependencies": {
    "simplecheckout-sdk": "^1.12.0"
  },
  "devDependencies": {
    "vite": "^7.2.7"
  }
}
